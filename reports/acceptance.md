# Отчёт приёмки — tender-collector 0.1.0 (обновление 2026-09-18, SAM включён)

## Статусы источников — все три LIVE_VERIFIED

| Источник | Статус | Доказательство |
|---|---|---|
| TED | **LIVE_VERIFIED** | 10 запросов; 36 тендеров + 20 awards; сверка 644140-2026 |
| UK CF | **LIVE_VERIFIED** | 10 запросов; 3+3; rate limit пойман и закрыт; сверка d578aaf8… |
| SAM.gov | **LIVE_VERIFIED** | ключ пользователя; 3 записи; сверка 3/3 (100% сечения) |

## SAM.gov — приёмка (обновление)

- Ключ: выдан пользователем на sam.gov (Account Details), положен в `.env`
  (git-ignored), автозагрузка в CLI, вырезается из raw/логов/экспортов.
- Live: `GET opportunities/v2/search`, ncode=541613, 30 дней →
  totalRecords=3, возвращено 3. NAICS 541613 — узкое сечение: больше
  записей за период у API нет; сверены **все 3 из 3** (по ТЗ 10 — взято
  100% доступного, без выдумок).
- Ручная сверка полей по каждой записи против raw:
  - noticeId → source_notice_id — точное совпадение (3/3);
  - title — точное совпадение (3/3);
  - naicsCode 541613 — 3/3;
  - postedDate → published_at UTC — 3/3;
  - fullParentPathCode → buyer_org_key — 3/3 после исправления бага
    (поле не заполнялось в тендерах — найдено сверкой, исправлено,
    покрыто тестом test_tender_keeps_explicit_buyer_key);
  - award: в сечении нет Award Notice — value_estimated=None честно.
- Keywords-фильтр (searches.example.json) пропускает 1 из 3 записей —
  локальный фильтр работает как заявлено.

## Исправления по итогам live-сверки

1. buyer_org_key в тендерах — не заполнялся (только в awards). Исправлено,
   добавлен тест.
2. raw-путь: `raw/` → `data/raw/` (синхронизировано с README/.gitignore),
   существующие raw перенесены.
3. Порядок вычисления org-ключей в normalize (UnboundLocalError) — исправлен.
4. Первый прогон после заполнения buyer_org_key дал ~39 «updated» —
   правдивые события (поле реально заполнилось); последующие повторы —
   +0/~0 по всем трём источникам (идемпотентность подтверждена).

## Тесты

**27/27 OK** (+1 новый: явный buyer_org_key в тендере).

## Итоговые данные

- Тендеров 42 (TED 36, UK 3, SAM 3), присуждений 23, организаций 83
  (поставщики 23 с wins_count и суммами по валютам).
- Экспорт: data/export/tenders.csv (42), awards.csv (23), orgs.csv (83).
- Raw: data/raw/{ted,sam,ukcf}/2026-09-18/ — gzip JSON, api_key вырезан.

## Общий статус

**COMPLETE по ТЗ**: все три источника LIVE_VERIFIED, сквозной путь
search → raw → normalize → SQLite → экспорт работает на реальных данных,
идемпотентность и честные статусы подтверждены live-прогонами.

Оставшиеся известные ограничения — в README (countries_ted не применяется,
keywords локальный, TED deadline/процедура недоступны в fields v3,
SAM — только федеральные США и один naics-код за запрос).
